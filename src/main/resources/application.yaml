spring:
  application:
    name: hidrat.ai

  datasource:
    url: jdbc:postgresql://localhost:5432/hidratai
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

